import React from "react";
import { capitalizeFirstLetter } from "../../utils/helpers";

function Nav() {
    return (
        <section className="my-5">
          <h1 id="about">Chao's nav</h1>
        </section>
      );
    }

export default Nav;